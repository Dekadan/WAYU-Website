---
import DocumentsIndex from '../../views/DocumentsIndex.astro';
import { useTranslations } from '../../i18n/utils';

const lang = 'en' as const;
const t = useTranslations(lang);
---

<DocumentsIndex
  lang={lang}
  title={t('nav.documents')}
  description="The constitution, declarations and congress resolutions that WAYU is built on."
/>
